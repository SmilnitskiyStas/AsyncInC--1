using System.Threading.Channels;

namespace ThreadToMeet
{
    internal class Program
    {
        public static void Main(string[] args)
        {
            //Learn_ThreadStart learn_ThreadStart = new Learn_ThreadStart();

            //Learn_ParameterizedThreadStart learn_ParameterizedThreadStart = new Learn_ParameterizedThreadStart();

            //SynchronizationThread thread = new SynchronizationThread();

            MonitorThreading monitorThreading = new MonitorThreading();
        }
    }
}